import 'package:flutter/material.dart';
import 'package:hnc_admin_dashboard/src/extension/color_extension.dart';

const secondaryColor = Colors.white;
final MaterialColor primaryColor =
    const Color(0xff282945).convertToMaterialColor();
final MaterialColor accentColor =
    const Color(0xffc0233b).convertToMaterialColor();
const bgColor = Color.fromARGB(255, 213, 217, 219);
const textColor = Colors.black;
const lightTextColor = Colors.black26;
const transparent = Colors.transparent;

const grey = Color.fromARGB(255, 164, 172, 192);
const purple = Color.fromRGBO(165, 80, 179, 1);
const orange = Color.fromRGBO(251, 137, 13, 1);
const green = Color.fromRGBO(51, 173, 127, 1);
const red = Colors.red;
const indigo = Color.fromARGB(255, 14, 19, 179);

// Default App Padding
const appPadding = 16.0;
